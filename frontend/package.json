{
  "name": "poker-frontend",
  "version": "0.1.0",
  "description": "Frontend for the TezosPoker project",
  "main": "main.js",
  "scripts": {
    "build": "rm -rf dist/* && cp src/libs/SRACryptoWorker.js dist/ && parcel build --no-minify src/index.html --public-url /static --no-source-maps --global pokerjs"
  },
  "browserslist": [
    "last 3 Chrome versions"
  ],
  "author": "",
  "dependencies": {
    "secure-shuffle": "^1.2.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
